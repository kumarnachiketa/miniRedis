# miniRedis server config (key=value or key value)
# Missing or invalid values use defaults.

port=6379
aof_file=aof.log
shards=64
# Set aof_fsync=no for higher throughput (no flush per write; less durable)
aof_fsync=every_write
